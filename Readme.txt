Dependencies: Git

run merge.bat as admin

after instalation commands (optional) e.g: install Packages      #only from WGET github repository!

     install "repository name" -e "username"        # repository name = name of something from github   #-e select the install from a custom repository      # username = Name of the person with the repository

e.g: install BurntToast -e Windos     #is corect the "Windos" 